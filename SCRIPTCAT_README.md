# Bilibili 中文字幕遮罩与场景复习本 v2.4.0

看双语视频时遮住中文，遇到想再听的句子，留下截图和时间；之后回到原视频复听、记笔记和做简单自评。适用于桌面 B 站内嵌双语字幕，不自动识别或翻译字幕。

## 安装

启用 Tampermonkey 或 ScriptCat 后，打开 [安装 / 更新脚本](https://raw.githubusercontent.com/black-customer/CaptionNoChinese/main/bilibili-caption-blur-mask.user.js)，确认安装并刷新视频页。

如果浏览器直接显示代码，可在管理器中新建脚本并粘贴完整内容。已有用户更新原条目，避免启用多个版本。自动更新检查由脚本管理器设置决定。

## 功能

- **中文遮罩：** 羽化模糊或实色遮挡，位置、尺寸与模糊强度可调；移入鼠标或按住 Alt 临时查看。
- **场景收藏：** 截图关联视频、分集 / 分 P 与时间；跨视频回听先打开正确来源。
- **片段循环：** 设置前后范围、次数、间隔与倍速；停止恢复当前视频开始循环前的进度。跨视频先回来源，页面就绪后再点一次循环。
- **复习本：** 搜索笔记、标题和标签；切换当前剧集 / 全部范围，筛选“待复习 / 仍不会 / 已掌握”，每页 30 条。
- **草稿与撤销：** 笔记延迟保存，失败保留页面草稿并可重试；可撤销最近一次删除。
- **备份与阅读：** 全量 JSON 备份，预览后导入，跳过已有 ID；筛选结果可导出为含图片和来源链接的离线 HTML。
- **设置与帮助：** 关闭 S / B 单键、调整回听提前量，随时查看操作帮助和版本。

## 常用操作

| 操作 | 功能 |
|---|---|
| `Alt + S` / `S` | 收藏场景 |
| `Alt + B` / `B` | 开关复习本 |
| `Alt + C` / `Shift + C` | 开关遮罩 |
| `Alt + Z` / `Shift + Z` | 遮罩编辑 |
| 播放器内 `Alt + 滚轮` | 上下微调 |
| 移入鼠标 / 按住 `Alt` | 临时查看中文 |

S / B 单键可关闭，输入框内不触发收藏。也可使用播放器控制条按钮。

## 保存与兼容边界

收藏保存在本机当前浏览器的站点数据中，清理站点数据或浏览器配置可能使其丢失，请定期 **备份全部**。JSON 包含收藏及图片，不包含脚本设置；HTML 阅读文件不能直接恢复收藏。

截图为最大宽度 **960 px、JPEG 质量 0.85** 的压缩图片。存储没有无限容量或永久保存保证。导入每次最多 **5,000 条、合计 100 MiB 数据、单图 10 MiB**；超过 150 MiB 的 JSON 文件在读取前拒绝。

受保护画面、跨域限制、播放器未就绪或 B 站改版可能影响适配。旧卡片缺来源时不能补出准确分集；没有可靠季信息时按分集归组。播放器原生全屏与网页全屏需要分别验收。

没有主动上传笔记功能，当前不含 OCR、自动翻译、账号或云同步。

[完整说明](README.md) · [更新记录](CHANGELOG.md) · [测试与真实站点验收](docs/ACCEPTANCE.md)
