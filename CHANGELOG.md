# 更新记录

兼容性与实测范围见 [验收记录](docs/ACCEPTANCE.md)。

## v2.4.0 — 2026-09-15

### 修复

- 数据写入与迁移等待事务结果，避免失败却报告成功。
- 历史迁移可重试，避免重复覆盖新笔记、恢复已删除卡片；失败保留恢复来源。
- 标题、笔记和标签采用安全文本渲染。
- 保存来源 URL 与分集 / 分 P 身份，跨视频先导航，再确认媒体就绪并定位。
- Canvas 图像与播放时间分开读取，多媒体共存时绑定相应播放器。
- 遮罩考虑实际画面与黑边，处理容器变化及全屏状态。
- 快捷键过滤输入、无效范围与重复按键。
- 笔记串行保存，晚返回查询和重绘不覆盖当前编辑；失败保留草稿。

### 新增

- 当前剧集 / 全部范围、关键词搜索、标签、复习状态与每页 30 条分页。
- 回听提前量、A–B 循环的前后范围 / 次数 / 间隔 / 倍速，以及停止后恢复当前视频的循环前进度；跨视频先导航，再手动开始循环。
- 隐藏笔记答案，回忆后再揭示。
- 全量收藏 JSON 备份、导入预览与跳过已有 ID 的恢复流程。
- 当前筛选结果的独立 HTML 阅读导出，保留图片、笔记和来源链接。
- 最近一次删除撤销、图片懒加载与对象 URL 释放。
- 模糊 / 实色遮罩、强度、位置、单键快捷键等设置，帮助与版本入口。
- 模块化开发源码、单文件安装产物、自动测试和 CI。
- 自动更新地址指向本仓库 `main`，保留原脚本名称与命名空间。

### 数据与文档边界

- 截图最大宽度 960 px、JPEG 0.85，不是原画无损。
- 导入限制为 5,000 条、元数据与解码图片合计 100 MiB、单图 10 MiB；读取前拒绝超过 150 MiB 的文件。
- JSON 用于恢复收藏，HTML 用于阅读；备份不含设置，不猜测旧卡片缺失的来源。
- 移除无限容量和永久保存承诺，补充首次安装、更新与故障排查。
- 不包含 OCR、自动翻译、账号或云同步。

## v2.2.0 — 历史基线

- 中文区域羽化遮罩、Alt 临时查看与滚轮位置微调。
- 截图收藏、时间戳、笔记抽屉与 Markdown 导出。
- IndexedDB 存储和首次使用引导。

[历史评审](docs/REVIEW_AND_ROADMAP.md) 保留 v2.2.0 的问题与改进建议。
